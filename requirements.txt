# Dependances directes de l'application. Les versions sont bornees par le haut
# sur la majeure pour eviter qu'un build futur ne tire une rupture d'API
# silencieusement. Le build reproductible passe par requirements.lock.txt,
# regenere apres toute modification ici :
#   uv pip compile requirements.txt --generate-hashes --python-version 3.11 \
#     --python-platform x86_64-manylinux_2_28 --no-header -o requirements.lock.txt

fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.22.0,<1.0.0
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.11.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0
pydantic[email]>=2.0.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
PyJWT>=2.7.0,<3.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt<5.1
celery>=5.3.0,<6.0.0
redis>=4.6.0,<7.0.0
python-dotenv>=1.0.0,<2.0.0
python-multipart>=0.0.7,<1.0.0
defusedxml>=0.7.1,<1.0.0
prometheus-client>=0.19.0,<1.0.0

# Client HTTP de app/parsers/crowdstrike.py (synchro Falcon Spotlight).
requests>=2.31.0,<3.0.0
